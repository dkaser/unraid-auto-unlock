version: "2"
linters:
  default: all
  disable:
    - wsl # Superseded by wsl_v5
    - depguard
    - err113
    - exhaustruct
    - forbidigo
    - gochecknoglobals
    - noctx
    - lll
  exclusions:
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
  settings:
    varnamelen:
      ignore-decls:
        - fs afero.Fs

formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines
    - swaggo
