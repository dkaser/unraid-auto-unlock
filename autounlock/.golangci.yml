version: "2"
linters:
  default: all
  disable:
    - wsl # Superseded by wsl_v5
    - depguard
    - err113
    - exhaustruct
    - forbidigo
    - gochecknoglobals
    - noctx
    - lll
    - testpackage
    - paralleltest
  exclusions:
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - path: _test\.go
        linters:
          - varnamelen
          - errcheck
          - goconst
  settings:
    varnamelen:
      ignore-decls:
        - fs afero.Fs

formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines
    - swaggo
